NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
NEXT_PUBLIC_ALLOWED_DEV_ORIGINS=scalding-imprecise-emcee.ngrok-free.dev
# Coma-separated hosts or full URLs for remote images used by next/image.
# Cloudinary is allowed by default. Add extra hosts only when needed.
# Example: NEXT_PUBLIC_IMAGE_REMOTE_HOSTS=https://mifoto.com,https://cdn.midominio.com
NEXT_PUBLIC_IMAGE_REMOTE_HOSTS=https://mifoto.com